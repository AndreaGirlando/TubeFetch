services:
  flask-api:
    build: TubeFetch.api/.
    container_name: TubeFetch.api
    ports:
      - "5000:5000"
    volumes:
      - ./songs:/usr/src/app/songs
    env_file:
      - .env
    restart: unless-stopped

